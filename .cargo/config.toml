[unstable]
json-target-spec = true
build-std-features = ["compiler-builtins-mem"] #memcpy, memset
build-std = ["core", "compiler_builtins"] # recompile core

[build]
target = "x86_64-luna_os.json"

[target.'cfg(target_os = "none")']
runner = "bootimage runner"